# v3.5 部署指南（修复托盘图标 + 标签页隐藏）

## 一句话总结

上一版构建后遗留的两个问题（**右下角托盘图标不显示**、**5 个浏览标签页没隐藏**）根源已找到并修复。
你这次**只需要换 1 个文件：`apply_custom.py`**，其他文件（build-windows.yml / RustDesk.toml / custom.json）都不用动，
换完重新跑一次构建即可。

---

## 这次修了什么（大白话）

| 问题 | 为什么上一版没修好 | 这次怎么修的 |
|------|------------------|--------------|
| **问题2：右下角没有托盘图标** | RustDesk 的托盘图标在 Windows 上是一个**独立的小程序**（`rustdesk.exe --tray`）。官方逻辑是：只有「服务已安装并在运行」时才会把它带起来。你用的是免安装便携版，永远不满足条件，所以图标从来不出来。上一版只改了「点关闭窗口时藏起来」，没管这个独立小程序 | 改成：**双击运行主程序时，自动把托盘小程序也带起来**。关掉主窗口（只是藏到后台），右下角图标还在；**点一下托盘图标就能把主窗口重新打开** |
| **问题3：5 个标签页还在** | 上一版的补丁是「按内容一字不差地找代码再替换」。你的仓库源码比较新，那一行代码多了几个字（多了个 `disable-discovery-panel` 判断），导致**没找着、补丁悄悄跳过了**，构建出来的还是带标签页的版本 | 两层保险：① 改成「按形状找」（不管内容怎么变都能找到并禁用全部标签）；② 直接把标签面板从界面布局里**整块移除** |
| 防再犯 | 上一版补丁没打上时**不报错**，白等 2 小时构建出没修复的版本 | 新增「**打完补丁立刻自检**」：任何关键改动没生效，构建**当场失败**并写明原因，不再白等 |

---

## 你要做的（只换 1 个文件，3 步）

### 第 1 步：替换 apply_custom.py

1. 下载并解压 `rustdesk-custom-win.zip`。
2. 把根目录的 **`apply_custom.py`**（v3.5，文件开头注释写着 v3.5）复制到你电脑上的 rustdesk 文件夹
   （GitHub Desktop → `Repository → Show in Explorer` 打开的那个）的**根目录**，**覆盖**旧的同名文件。
3. GitHub Desktop → 左边显示 1 个文件被修改 → Summary 随便写 `v3.5` → **Commit to main** → **Push origin**。

> 其他文件（`.github/workflows/build-windows.yml`、`RustDesk.toml`、`custom.json`）这次**都不用换**，
> 跟上次一样放着就行。想一起换也可以（内容没有变化，只有注释更新）。

### 第 2 步：重新跑构建

1. 打开你的仓库网页 → 顶部 **Actions**。
2. 左边选 **Build Custom RustDesk (Windows)**。
3. 右上角绿色 **Run workflow → Run workflow**（⚠️ 一定要跑**新的**，别在旧记录上点 Re-run）。

### 第 3 步：下载使用

- 照旧等 1.5～2 小时，绿色 ✔ 后下载 **Artifacts → rustdesk-windows**，解压，双击 `rustdesk.exe`。
- 验证两处：
  - **右下角托盘出现 RustDesk 图标**（如果被收进「^」折叠区，点 ^ 展开看）；
  - 主界面**只剩「你的桌面」+「控制远程桌面」**，下方 5 个标签页（最近访问过/收藏/已发现/地址簿/可访问的设备）全部消失。

---

## 装好后的使用方式（重要，先知道）

- **关闭主窗口 ≠ 退出程序**：点主窗口右上角 ×，程序只是藏到右下角托盘，**别人依然可以远程连你**。
- **想再打开主窗口**：点一下右下角的 RustDesk 托盘图标（左键单击即可）。
- **想让别人连不上**：暂时没有「一键退出」。最简单的办法是**重启电脑**（开机后不双击 rustdesk.exe 就不会自动运行），
  或在任务管理器里结束 `rustdesk.exe` 和 `RustDesk` 开头的进程。

> 如果这次构建**失败**了：说明自检发现源码又变了，请照旧把「标红步骤名字 + 日志最后 20~30 行」发我，
> 特别是 `Apply custom patch` 这一步里 `校验补丁结果` 打印的几行。这正是自检在工作——不会再让你白等 2 小时。
